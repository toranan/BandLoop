{
  "name": "bandloop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-website.mjs",
    "check": "npm --prefix Website run check"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
